<UserControl 
    x:Class="Othello.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300"
>
    <UserControl.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource MainTextBlockStyle}" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="80"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Image black pawn : " />
        <TextBox Grid.Row="0" Grid.Column="1" IsEnabled="False"/>
        <Button Grid.Row="0" Grid.Column="2" Content="Browse" />
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Image white pawn : " />
        <TextBox Grid.Row="1" Grid.Column="1" IsEnabled="False"/>
        <Button Grid.Row="1" Grid.Column="2" Content="Browse"/>
    </Grid>
</UserControl>
