<UserControl 
    x:Class="Othello.Views.RibbonBarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:Othello.Views"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="300"
>
    <StackPanel Orientation="Horizontal" Margin="20,0,0,0" VerticalAlignment="Center">
        <ItemsControl ItemsSource="{Binding ViewModel.RibbonItems, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:RibbonBarView}}}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding Action}" Width="32" Height="32" Padding="0" Margin="15,0,0,0" Background="Transparent">
                        <Image Source="{Binding ImageSource}" Width="32" Height="32"/>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </StackPanel>
</UserControl>
